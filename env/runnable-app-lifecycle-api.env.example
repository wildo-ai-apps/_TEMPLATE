# =============================================================================
# APP LIFECYCLE API ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to runnable-app-lifecycle-api.env and configure your values
# =============================================================================

# Service Configuration
NODE_ENV=development
HOST=0.0.0.0
PORT=4236
SERVICE_NAME=runnable-app-lifecycle-api

# Application Identity
APPLICATION_ID=__APPLICATION_ID__
APP_SLUG=__APP_SLUG__
APP_NAME=__APP_NAME__
APPLICATION_ENV=development

# Platform Configuration
PLATFORM_APPLICATION_PRIMARY_SECRET=__PLATFORM_PRIMARY_SECRET__
PLATFORM_ADMINISTRATION_URL=http://host.docker.internal:5151

# Apps Updater Public Key (for verifying incoming requests from platform-apps-updater)
# Uses ECC P-256 keys with ES256 algorithm
# Format: single line with escaped newlines, e.g., "-----BEGIN PUBLIC KEY-----\nMFkw...\n-----END PUBLIC KEY-----\n"
APPS_UPDATER_PUBLIC_KEY=__APPS_UPDATER_PUBLIC_KEY__

# App Custom Root Directory (for file operations)
# Fixed path - Docker binds apps-custom/${appSlug} to apps-custom/${APP_SLUG}
APP_CUSTOM_ROOT=/app/apps-custom/__APP_SLUG__

# Logging
LOG_LEVEL=debug
WILDO_FORMATTED_CONSOLE_ERRORS=true

