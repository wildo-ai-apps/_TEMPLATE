# =============================================================================
# FRONTEND MAIN APP ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to runnable-frontend-main-app.env and configure your values
# =============================================================================

# Service Configuration
NODE_ENV=development
PORT=4242
VITE_PORT=4242
# HMR client port for Docker: set to external port (internal + PORT_OFFSET)
VITE_HMR_CLIENT_PORT=4242

# Application Identity
VITE_APP_SLUG=__APP_SLUG__
VITE_APP_NAME=__APP_NAME__
VITE_APP_TYPE=main

# API Configuration
VITE_API_BASE_URL=http://localhost:4241
VITE_API_BASE_PATH=/api/v1

# WebSocket Configuration
# DEPRECATED: VITE_WS_URL is no longer used. WebSocket URL is derived from 
# the VITE_API_BASE_URL at runtime (same endpoint, Socket.IO handles the upgrade).
# Kept for backwards compatibility, will be removed in a future version.
VITE_WS_URL=ws://localhost:4241

# Feature Flags
VITE_ENABLE_DEBUG=true
VITE_ENABLE_DEVTOOLS=true

